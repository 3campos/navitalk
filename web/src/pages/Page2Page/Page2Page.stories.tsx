import type { Meta, StoryObj } from '@storybook/react'

import Page2Page from './Page2Page'

const meta: Meta<typeof Page2Page> = {
  component: Page2Page,
}

export default meta

type Story = StoryObj<typeof Page2Page>

export const Primary: Story = {}
