import type { Meta, StoryObj } from '@storybook/react'

import AskALocalPage from './AskALocalPage'

const meta: Meta<typeof AskALocalPage> = {
  component: AskALocalPage,
}

export default meta

type Story = StoryObj<typeof AskALocalPage>

export const Primary: Story = {}
